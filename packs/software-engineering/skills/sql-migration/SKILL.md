---
name: sql-migration
description: Create a versioned SQL migration and related queries using the project's migration mechanism. Use when the user asks for a database schema migration or invokes the /migration adapter.
---

# SQL Migration

Read `references/workflow.md`, then follow the migration and data-access conventions discovered in the repository. Never run rollback, reset, or production migration operations without concrete authorization.
