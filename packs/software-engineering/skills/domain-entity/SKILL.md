---
name: domain-entity
description: Implement a domain entity through the established persistence, service, API, and UI layers. Use when the user explicitly requests a full entity flow or invokes the /entity adapter.
---

# Domain Entity

Read `references/workflow.md`, then follow the project's existing architecture. Treat its stack-specific content as examples rather than global requirements.
