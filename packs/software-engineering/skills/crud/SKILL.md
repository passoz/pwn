---
name: crud
description: Create a complete CRUD feature across the project's established layers. Use when the user explicitly asks for CRUD implementation or invokes the /crud adapter.
---

# CRUD

Read `references/workflow.md`, then adapt it to the repository's actual stack and conventions. Treat stack-specific examples as historical guidance, not permission to impose a stack.
