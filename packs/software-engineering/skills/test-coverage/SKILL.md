---
name: test-coverage
description: Analyze test coverage for a change and add missing regression tests using the repository's existing harness. Use when the user requests coverage analysis or invokes the /test-coverage adapter.
---

# Test Coverage

Read `references/workflow.md`, then use the project's real coverage commands and thresholds. Do not invent an 80% threshold, install a harness, or impose Go-specific patterns unless the repository already requires them.
