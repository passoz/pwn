---
description: Create a domain entity flow using the installed skill.
argument-hint: "[-h | entity] [fields]"
---

Load the installed skill `domain-entity` and execute the stage or workflow corresponding to this adapter. Pass `$@` through unchanged as the user's explicit arguments. The skill owns help behavior, discovery, execution, evidence, and completion rules; do not duplicate or replace them here.
