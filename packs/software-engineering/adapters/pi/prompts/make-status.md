---
description: Show a read-only project panorama with all task and evidence states.
argument-hint: "[-h | NNNN | plan path | legacy] [--json]"
---

Load the installed skill `engineering-workflow` and execute the project status stage corresponding to this adapter. Pass `$@` through unchanged as the user's explicit arguments. The skill owns help behavior, task parsing, evidence inspection, status precedence, and output formatting; do not duplicate or replace them here.
