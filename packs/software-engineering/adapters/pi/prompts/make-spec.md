---
description: Create or reconcile a trustworthy system-wide behavioral specification.
argument-hint: "[-h | system description | output.md] [--reconcile]"
---

Load the installed skill `system-specification` and execute the workflow corresponding to this adapter. Pass `$@` through unchanged as the user's explicit arguments. The skill owns help behavior, repository discovery, reconciliation, validation, and completion rules; do not duplicate or replace them here.
