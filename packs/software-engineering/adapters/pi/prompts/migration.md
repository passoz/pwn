---
description: Create a versioned SQL migration using the installed skill.
argument-hint: "[-h | table] [columns]"
---

Load the installed skill `sql-migration` and execute the stage or workflow corresponding to this adapter. Pass `$@` through unchanged as the user's explicit arguments. The skill owns help behavior, discovery, execution, evidence, and completion rules; do not duplicate or replace them here.
