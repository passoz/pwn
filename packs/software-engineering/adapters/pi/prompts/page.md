---
description: Create a React page using the installed skill.
argument-hint: "[-h | name] [route]"
---

Load the installed skill `react-page` and execute the stage or workflow corresponding to this adapter. Pass `$@` through unchanged as the user's explicit arguments. The skill owns help behavior, discovery, execution, evidence, and completion rules; do not duplicate or replace them here.
