---
description: Analyze and improve test coverage using the installed skill.
argument-hint: "[-h | scope] [commit-ref]"
---

Load the installed skill `test-coverage` and execute the stage or workflow corresponding to this adapter. Pass `$@` through unchanged as the user's explicit arguments. The skill owns help behavior, discovery, execution, evidence, and completion rules; do not duplicate or replace them here.
